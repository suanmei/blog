import React from 'react';
import Layout from '../component/layout';

export default () => (
  <Layout title={ '拾邑' }>
    <section>
      <h1>博客文章</h1>
      <i className={ 'icon' }></i>
    </section>
  </Layout>
);
